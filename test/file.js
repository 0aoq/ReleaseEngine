define(["require", "exports"], function(require, exports) {
    // the function below runs only once the module is required
    exports.main = function() {
        "test loaded"
    }

    // the function below (if it was uncommented) would run every frame (0.1s)
    /* exports.update = function() {

    } */
})